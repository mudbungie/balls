+++
title = "prime debris: a crashed founding's .git/index.lock blocks the re-run's git add"
created = 1785125228
updated = 1785129413
priority = -1
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"

[[blockers]]
id = "bl-1d99"
on = "close"
+++
bl-ffbf made founding re-runnable over crash debris (predicate = commit on balls/config, pre-commit steps overwrite). Remaining sliver: if the crash left .git/index.lock in the landing clone, the re-run's git add fails with git's raw lock error. The predicate change handles the common window; this is the narrow one. Candidate: prime's debris report names the stale lock and the removal (matching the existing debris-report voice), rather than founding deleting locks itself (a lock that is LIVE — another process mid-op — must not be deleted; prime can only advise, per 'prime never deletes what may hold work'). Wording + a test.