+++
title = "Stress the plugin-protocol defensive machinery"
created = 1784524140
updated = 1784524225
claimant = "Robber"
priority = 2
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]
+++
Integration-test coverage gap (audit bl-covtest, grade C-).

**Target file:** `tests/protocol_edges.rs`

**Exercises:** Set BALLS_PLUGIN_DEPTH at DEPTH_CAP and run a hooked op; an unbound plugin with no [source] hint; a synthetic A-succeeds/B-fails pair asserting A's rollback phase runs with the rolling_back wire tag; two marker-stamping plugins proving list-order execution; a failing read-op (list/show) hook plugin; read the clones/<enc>/log for the enveloped stderr record, RELAY_LINE_MAX truncation, and the threshold-surviving error record; assert a freshly primed claim.post omits bl-chore.

**Assertion:** Depth-cap aborts loudly naming op/phase/plugin (never silently plugin-free); the no-hint abort says 'run bl install'; rollback order is reverse with the tag observed on B's wire; stamps match configured order; the failed read still renders; log records carry src/lvl/op/phase and the truncation marker; default schedule excludes bl-chore.

**Priority:** med. Rules: touch ONLY tests/ (never src/ — keeps the 100% tarpaulin gate green); each .rs file <=300 lines; drive the real bl binary via assert_cmd.