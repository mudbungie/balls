+++
title = "Realign the [source] doc examples to the real first consumers: the design record (bl-5b09 §1 example table, §2 message examples, Touches) and the architecture §6/§15 passages, README, skill/install.md, and the hooks.rs module doc all illustrate with bl-adversary = 'cargo install balls-adversary' — a plugin name and acquisition command that do not exist (the real plugin is 'adversary', not on crates.io, acquired by git clone + make install; the gh issues plugin is 'github-issues', one-command since bl-6ebc renamed its installed binary to the schedule name). Never assume a doc is correct: amend the examples to the entries actually authored on the balls center landing 2026-07-07 — adversary = 'git clone https://github.com/mudbungie/balls-adversary && make install', github-issues = 'git clone https://github.com/mudbungie/balls-github-plugin && make install' — keeping every message FORMAT string unchanged (the code interpolates name/hint; only the illustrative names were wrong). Docs-only."
created = 1783397706
updated = 1783397715
claimant = "Wagered"
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
+++
