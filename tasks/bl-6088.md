+++
title = "Cover the list query surface end-to-end"
created = 1784524137
updated = 1784524219
claimant = "Robber"
priority = 3
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]

[[blockers]]
id = "bl-3034"
on = "close"
+++
Integration-test coverage gap (audit bl-covtest, grade C-).

**Target file:** `tests/reads_list.rs`

**Exercises:** `bl list -s blocked`, `--all`/`-s closed` after a real close, `--tag` (AND-composed), `--claimant` (live and with -s closed), `--since/--until` windows, positional NEEDLE, explicit `--plain`, the claim-age '(<age>)' suffix on -s claimed rows, and `list --legacy + --all` contradiction.

**Assertion:** Each filter's stdout contains exactly the expected ids and excludes the others; a closed task reappears under --all with its history-reconstructed row; claimed rows carry an age suffix; the --legacy+--all combination exits with the usage error.

**Priority:** high. Rules: touch ONLY tests/ (never src/ — keeps the 100% tarpaulin gate green); each .rs file <=300 lines; drive the real bl binary via assert_cmd.