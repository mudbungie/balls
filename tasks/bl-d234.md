+++
title = "Demo: central/satellite federation (shared store branch)"
created = 1780970031
updated = 1780970031
parent = "bl-9369"
priority = 2
tags = ["review"]
+++
One store branch, multiple landings (federation = many landings sharing one store branch, §12). Prime a satellite with --remote against a center; create on one side, confirm `bl sync` converges the other; verify push/fetch is tracker-only and that store-pointer resolution precedence holds (--remote > --center > XDG > origin). Record the transcript.