+++
title = "Interface ergonomics review (CLI/streams/errors)"
created = 1780970013
updated = 1780970013
parent = "bl-72a8"
priority = 1
tags = ["review"]
+++
Review the CLI surface for ergonomics. Verify the output-stream contract holds for EVERY verb: only create→stdout (bare id), mutations→stderr, --json = bedrock (raw frontmatter, literal i64 ts, no derived fields). Audit error/refusal messages (claim of a blocked task NAMES the blocker; point-of-use missing-bin hint; close-from-worktree guidance). Check flag-pair symmetry (--needs/--no-needs, -t/--no-tag, set/clear) and help text.

Deliverable: UX findings + fixes; confirm SKILL.md matches real behavior.