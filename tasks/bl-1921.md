+++
title = "gh-plugin: conformance & dead-code sweep (cut return-channel model, fix doc citations)"
created = 1780996131
updated = 1780996131
parent = "bl-10a4"
priority = 1
tags = ["gh-plugin"]
+++
Bring interfaces to §0–§17. (1) CUT dead code: balls-github-shared/src/types.rs (the old return-channel Task/SyncReport/SyncCreate/Update/Delete model that §7 'no return channel' abolished — zero importers), projection_boundary_test.rs, the 'pub mod types' decl, and scrub SyncReport/'deserialize a Task' vocab from shared/lib.rs. (2) Fix doc-citation drift: sync-cwd comments cite §8; correct is §13 (sync cwd = store) / §7 (wire) — in pull.rs, store.rs, push.rs. (3) Stale paths/vocab: config.rs '.balls/plugins/*.json' → 'config/plugins/<name>.json' (both crates); drop 'deferred-mode review' wording (forge config.rs); fix shellback id_length comment (§16: id scheme fixed); README 'Migrating' clones/<clone> path → mirrored landing path. Verify drop→close-issue is correct (drop is terminal in greenfield, not a misfire).