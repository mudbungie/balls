+++
title = "bl-upstream plugin (sibling repo): a shared bl store as a FOUNDED checkout in the plugin's territory, operated only via bl -C; up:<store>#<id> tags; four hooks"
created = 1789702124
updated = 1789705960
claimant = "Waitresses"
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"

[[blockers]]
id = "bl-aac7"
on = "claim"

[[blockers]]
id = "bl-331a"
on = "claim"

[[blockers]]
id = "bl-b863"
on = "close"
+++
Design: docs/design/bl-3616-store-tiers.md (CONVERGED 2026-09-17). §5. New sibling repo like balls-github-plugin. Founding: bl -C $XDG_STATE_HOME/balls/plugins/bl-upstream/<remote>/ prime --remote <shared> (the §1 keyed-by-path layout; no plumbing writes to a branch, ever — maintainer Q3: 'plumbing under the existence of other controls is how bugs get in'). Hooks: create/update/close.post → for up:-tagged balls, bl show <id> --json | bl -C <dir> import (close propagation = plugin policy: mirror closes the counterpart, component annotates); sync → bl -C <dir> sync then fold newer counterparts down (refuse with diff on conflict, seen-token discipline); show/list → drift from the two updated stamps. Wired on *.post = mandatory replication; on sync.pre alone = check-in on demand. Blocked on the H1 fill (a nested bl -C on another store must publish) and the tag charset. 100% coverage + 300-line cap per repo convention.