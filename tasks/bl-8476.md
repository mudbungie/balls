+++
title = "Two clones of one project collaborate through a shared origin"
created = 1784525380
updated = 1784525390
claimant = "Revises"
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]
+++
The textbook two-developers story: two independent bl-primed clones of the SAME project (identical root_commit), sharing the task store via origin — no --center. A creates/claims/closes; B syncs and sees it; both create concurrently and both land after mutual sync. Also assert prime prunes the settled work/<id> branch through the real CLI (skill/prime.md promise, currently only plugin-level tested). All existing two-writer tests use raw git as the second party or distinct-root fleet repos — the ordinary same-root convergence loop has no test. New file tests/two_clone.rs.