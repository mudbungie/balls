+++
title = "Root-set admit on a merged history; a center that declares stealth for the team"
created = 1784525386
updated = 1784525386
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]
+++
Two substrate promises: (1) Root-SET admit through the real CLI — create/claim a ball under sole root A, then git merge --allow-unrelated-histories an orphan history (root B) into the PROJECT repo; list/show/claim must still admit the pre-merge ball regardless of rev-list root order (unit-tested only; the live dispatch git-root read is unproven). (2) A center whose committed config declares task_remote = "none" (team-wide declared stealth): a satellite enrolling via prime --center/--install inherits the sentinel — post-adopt the satellite stays stealth on later ops with no durable binding write of its own (docs call this composition load-bearing; the halves are tested, the composition never). Extend tests/substrate_edges.rs + tests/enrollment.rs (this ball owns both; 300-line cap).