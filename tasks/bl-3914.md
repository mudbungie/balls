+++
title = "Document the global --log-level in the skill guide (resolves -q/--quiet as a duplicate)"
created = 1780984555
updated = 1781033267
claimant = "Lantern"
priority = 3
tags = ["ergonomics"]
delivery-worktree = "/home/mark/.local/state/balls/plugins/bl-delivery/home/mark/dev/balls/bl-3914"
+++
REFRAME (Outsmarted, 2026-06-09): the original ask — a global -q to silence the JSON op-log — is already covered by an existing explicit signal: the global --log-level flag (src/lib.rs strip_log_level, "the only global flag"; src/log.rs: CLI --log-level ▸ XDG ▸ landing ▸ default info). `bl --log-level warn <verb>` should yield exactly the asked-for behavior: info op-log silenced, terse stderr confirmations (plain eprintln, not log records) and stdout intact. Per the config-knobs-earn-their-keep principle, do NOT add -q.

DELIVERABLE: (1) VERIFY the claim — run a mutating verb with --log-level warn and confirm the JSON lines vanish while the terse confirmation survives; if the confirmation is itself a log record, the reframe fails and the original -q ask stands (reopen the discussion instead of landing). (2) If verified, add one SKILL.md line documenting --log-level for clean scripting (the bl-1b49 demo grep-dance pain point), deliver via the normal worktree flow, close.

ORIGINAL ASK (for the record): every verb writes the JSON op-log to stderr alongside the terse confirmation; scripts must wrap every call with 2>/dev/null or grep -v. Proposed a global -q/--quiet. Observed during the bl-1b49 E2E demo.