+++
title = "Kill -9 mid-close: the crash converges on retry, never limbo"
created = 1784525386
updated = 1784525400
claimant = "Revises"
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]

[[blockers]]
id = "bl-202f"
on = "close"
+++
PROBE. §14 converge-on-retry exists for the failure mode where core itself dies — nothing runs, not even rollback. Every existing "crash" test is a scripted plugin exit or a hand-made empty debris dir. Do it for real: wire a slow/fifo-blocked plugin into close, SIGKILL the bl process mid-flight, then assert (1) the task is in exactly one of {still-claimed, delivered} — never both-gone limbo; (2) a retried bl close converges without a duplicate squash; (3) bl prime reports the real debris (whatever a genuinely killed close leaves — changes/<uuid>, half-materialized state) rather than choking. New file tests/crash_recovery.rs.