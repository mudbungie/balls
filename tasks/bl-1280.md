+++
title = "Forge delivery variant + github-issues plugin port"
created = 1780648103
updated = 1780897431
claimant = "Gladdened"
parent = "bl-72a8"
priority = 4
delivery-worktree = "/home/mark/.local/state/balls/plugins/bl-delivery/%2Fhome%2Fmark%2Fdev%2Fballs/bl-1280"

[[blockers]]
id = "bl-f813"
on = "claim"

[[blockers]]
id = "bl-be5f"
on = "claim"

[[blockers]]
id = "bl-0802"
on = "claim"

[[blockers]]
id = "bl-189b"
on = "close"

[[blockers]]
id = "bl-613d"
on = "close"

[[blockers]]
id = "bl-2a81"
on = "close"
+++
Forge variant (approval gate child at claim.post; push work/<id> + PR at close.pre; forge sync closes gate on merge). Port github-issues to the subprocess protocol + one-time legacy-adoption seeding plugin scratch from old issue numbers (§17). Forge/issues plugins live in the sibling balls-github-plugin repo.

## Notes (migrated)

- 2026-06-06T04:24:43.365724861Z Lifeline: Scoped but not started (deferred to a later pass). Findings for next agent:
(1) Was blocked on bl-bdeb (verb dispatch run()->engine) — now CLOSED, so the forge variant's core mechanism (shell 'bl new --gates <parent>' at claim.post; 'bl close <gate-child>' at sync) is now runnable. Re-verify bl new/claim/close actually execute before relying on them.
(2) Ball bundles THREE separable deliverables, all in sibling repo /home/mark/dev/balls-github-plugin: (a) forge delivery variant — new-protocol plugin: approval gate child at claim.post, push work/<id>+PR at close.pre (NO local squash), forge sync closes gate on merge, rollback claim.post drops the gate child, rollback close.pre no-op, empty deliverable auto-closes its own gate child (§11 FORGE); (b) port github-issues from old clap CLI (push/sync --task + SyncReport on stdout) to §6/§7 subprocess protocol (<op> <phase> + wire on stdin, NO return channel — mutate worktree or shell bl); (c) §17 one-time legacy-adoption seeding plugins/github-issues/<id>/ XDG scratch from old external.github-issues.issue.number (zero-dup on next sync; skip-cost is 1 dup/task).
(3) No greenfield bl is installed (epic forbids make install until cutover) so sibling-repo integration tests can't exec a real bl — port plugins against fakes/seams like delivery's FakeRepo, or wait for cutover. Reference new-protocol plugins: src/bin/bl-delivery.rs + src/delivery.rs (dispatch matrix) + src/bin/tracker.rs.
(4) Consider splitting into sub-balls (forge / issues-port / §17-adoption) — likely too large for one ball.
- 2026-06-06T23:03:30.810716572Z Besting: AFFECTED by config/store split: the forge/delivery + github-issues plugins read the §7 binding, whose fields changed (operating → store/landing/tasks_branch — bl-f970); and their committed config crosses by install = path-copy (folder=mirror/glob=union, no object enum — bl-3dd9). The forge gate child + approval flow are unchanged. bl-2e26 §6/§7/§11.
- 2026-06-07T08:47:04.883706874Z Unit: REFRAMED 2026-06-07 (user): backed out of tying bl-1280 INTO the migration finale. The old edge (bl-0802 §17-migration depended ON bl-1280) is REMOVED. Reversed: bl-1280 now DEPENDS ON bl-0802 (full migration-workflow delivery: §16 script + dogfood install + cutover), which in turn now depends on the new center spec (bl-6965). Rationale: migration workflow + 'center' (shared hub: store-branch sharing + capability publish/adopt via install + cutover target) are core machinery delivered FIRST; bl-1280 (forge variant + github-issues port + its §16 plugin-adoption) is the downstream DEMO of that machinery and 'will be a good demo experience'. Do NOT implement bl-1280 until bl-0802 (and transitively bl-6965) land. Three deliverables + the in-flux §7 binding (bl-f970 family) unchanged from prior notes.
- 2026-06-07T19:52:42.401529723Z Unlearn: NOTE (bl-8540, merged e9b44cc): plugin wiring is now config/plugins.toml [hooks] (an ORDERED list of names per <op>.<phase>), not the symlink registry (§6). The forge variant registers its hooks (claim.post approval-gate child; close.pre push + PR) as plugins.toml [hooks] entries, and the github-issues port likewise. Wire forge into the seed's plugins.toml or via 'bl install', NOT config/plugins/<op>/<phase>/NN-<name> symlinks.
