+++
title = "Concurrent closes in one clone must both land on main"
created = 1784525381
updated = 1784525391
claimant = "Revises"
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]

[[blockers]]
id = "bl-6359"
on = "close"
+++
PROBE. src/delivery_repo_acts.rs::deliver computes parent = rev-parse(integration) then commit-tree then update-ref with NO old-value check — a non-atomic check-then-act. Two agents sharing one checkout closing two different tasks at once may silently drop one delivery off main (recoverable only via reflog, unreported). Force the interleave deterministically: task A pre-commit hook sleeps/waits on a fifo while task B closes to completion, then A finishes. Assert BOTH [bl-id] squashes are ancestors of final main. If the test proves the drop, that is a REAL FINDING to raise, not a test to delete — pin current behavior with a loud comment or land alongside a fix (update-ref has a compare-and-swap form: git update-ref ref new old). New file tests/close_race.rs.