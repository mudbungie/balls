+++
title = "A third-party veto plugin in the real close chain, and the append-after-tracker footgun"
created = 1784525384
updated = 1784525397
claimant = "Revises"
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]
+++
PROBE. skill/conf.md documents the one ordering footgun in the book: conf append close.post lands a plugin AFTER bl-tracker, so its failure un-seals a commit the remote already has — the next push rejects non-ff. Nothing tests a custom plugin interleaved with the REAL shipped chain (all protocol tests use synthetic plugins on create.pre in an otherwise-empty schedule). (1) conf prepend close.pre myveto (exit 1): close fails, main never moves, worktree up, task claimed — the adversary-plugin pattern every gated team relies on. (2) conf append close.post myveto against a real remote: demonstrate the documented footgun actually manifests as described. If the footgun is nastier than documented, that is a finding to raise. New file tests/veto_chain.rs.