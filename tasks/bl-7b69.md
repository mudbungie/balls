+++
title = "Run prime's converge and debris reporting through the real binary"
created = 1784524139
updated = 1784524223
claimant = "Robber"
priority = 2
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]

[[blockers]]
id = "bl-8906"
on = "close"
+++
Integration-test coverage gap (audit bl-covtest, grade C-).

**Target file:** `tests/converge_e2e.rs`

**Exercises:** Seed a landing plugins.toml naming retired 'tracker' ([hooks] + [source]) and run `bl prime`; repeat with the old name live-bound to a third-party binary; create a stray changes/<uuid>/ dir and a stealth.lock (with and without the declared-stealth sentinel); also dispatch a live op against a schedule still naming 'tracker' to observe the skip notice.

**Assertion:** prime rewrites to bl-tracker in one landing commit, binds the name, drops the dangling symlink (and leaves a live-bound name untouched); debris lines name `git worktree remove <path>` and the stealth.lock fix, suppressed once declared; live dispatch skips 'tracker' with the non-fatal rename notice and the op succeeds.

**Priority:** med. Rules: touch ONLY tests/ (never src/ — keeps the 100% tarpaulin gate green); each .rs file <=300 lines; drive the real bl binary via assert_cmd.