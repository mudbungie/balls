+++
title = "Wire the bl install verb (§6/§13) — collapses P3+P4"
created = 1780977333
updated = 1781031783
claimant = "Winch"
parent = "bl-72a8"
priority = 1
tags = ["review"]
delivery-worktree = "/home/mark/.local/state/balls/plugins/bl-delivery/home/mark/dev/balls/bl-f387"

[[blockers]]
id = "bl-ff89"
on = "claim"

[[blockers]]
id = "bl-2d6d"
on = "claim"

[[blockers]]
id = "bl-5cf3"
on = "claim"
+++
Wire the standalone `bl install` verb — the §6/§13 capability transfer. The install MODULE (src/install.rs) is fully built and unit-tested: `install(path, from, to)` (shape decides: folder = mirror with deletions propagating, file/glob = additive union, bin/ never travels), `referenced()`, and `resolve_and_bind()` (protocol-validated bind). The GAP is only the run-wiring: `Verb::Install` in lib.rs:run() currently falls through to `other => Op::new(other).plan()` — so `bl install` just prints its op plan and does nothing.

Implement `install::run(edge, args)`: parse `<path> --from <ref> --to <ref>` (default path = DEFAULT_PATH "config"), ref-materialize the two checkout roots, stage via `install::install`, SEAL through the Engine (commit on `--to`'s CURRENT tip swapping only `<path>` — never a whole-tree replace), then `referenced()` + `resolve_and_bind()` for the bind worklist, print the `Summary` to stdout. Dispatch `Verb::Install => install::run(...)`. REUSE the exact module fns `prime --install` already calls (single source of truth) — do not fork a second copy path; converge the two on the shared run-wiring where it's clean.

Collapses P1's siblings P3+P4 from the minimalism review (bl-004c): wiring the verb removes the LAST caller of the `other =>` arm, so `Op::plan` / `Op::new(other)` / the two `*_PHASES` arrays / `Phase::token` (src/op.rs) become dead — remove them in this ball (or keep ONE as a deliberate `--plan` introspection with a real caller; default is subtract). This ball IS the resolution of P4 (the unwired verb).

Gated behind the in-flight code reviews (bl-ff89 dead-code, bl-2d6d security, bl-5cf3 standards) since all touch lib.rs/op.rs — run on a settled tree, reconcile against current main.

Deliverable: `bl install <path> --from <ref> --to <ref>` works end-to-end (adopt = anvil→landing, publish = landing→center), prime --install still green, dead op-plan skeleton removed, gate green (100% coverage, clippy clean).