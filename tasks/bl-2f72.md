+++
title = "Exercise the occupancy and identity guard refusals"
created = 1784524136
updated = 1784524136
priority = 3
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]
+++
Integration-test coverage gap (audit bl-covtest, grade C-).

**Target file:** `tests/guards.rs`

**Exercises:** `bl claim` on an already-claimed ball; `bl unclaim`/`bl close` with a mismatched --as; a second agent's claim after a legitimate unclaim.

**Assertion:** Double-claim exits nonzero with stderr containing 'already claimed by <who>'; non-claimant unclaim/close are refused; the store state (claimant field via show --json) is unchanged after each refusal.

**Priority:** high. Rules: touch ONLY tests/ (never src/ — keeps the 100% tarpaulin gate green); each .rs file <=300 lines; drive the real bl binary via assert_cmd.