+++
title = "Re-apply bl-ffaf (delete dep-tree): bl-33db's close resurrected it via a modify/delete conflict resolved the wrong way"
created = 1781040045
updated = 1781040046
claimant = "Slimness"
+++
a9fcbb22 [bl-ffaf] deleted dep-tree (its --json duplicated list --json; the verb owned no machine contract). bl-33db's worktree branched before that delivery and touched the read files, so its close's main-fold hit modify/delete conflicts on src/reads/tree.rs (+ content conflicts in the siblings) that were resolved by keeping the work side — a7af2625 restored the verb, byte-identical to pre-deletion, plus all the doc rows and the spec text, and dropped bl-ffaf's §15 entry. Pure merge fallout, no design intent. Re-apply by cherry-picking a9fcbb22 onto fresh main; the only files with genuine bl-33db deltas are tests/dispatch.rs, SKILL.md, docs/architecture.md, docs/demonstration.md — resolve those by hand, keep both features.