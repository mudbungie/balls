+++
title = "Authoring guard rails through the real binary"
created = 1784525383
updated = 1784525383
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]
+++
The misuse envelope an LLM agent actually hits, all currently unit-only: update <id> --subtask-of/--blocks refused (create-only); create --blocks close with no --parent/--subtask-of refused with the pointer; --needs id:bogus_op refused naming the op; claim/close with field flags ("takes no field edits") refused; bl create -- "-p not-a-flag" creates that literal title byte-exact (the documented untrusted-input pattern); --blocks/--subtask-of edge-target liveness (unknown vs already-closed — the likelier LLM hallucination is a bad --subtask-of); single-call cycle create "verify" --subtask-of E --needs E refused with no orphan file left; update --edit under piped stdin fails fast (a hang would strand an unattended agent) and --edit+--title refused. After every refusal assert the store is unmutated. New file tests/authoring_guards.rs.