+++
title = "Doc update: register bl-chore in architecture.md §6/§11 + SKILL + README"
created = 1781809870
updated = 1781830221
parent = "bl-6ee9"
priority = 3

[[blockers]]
id = "bl-c370"
on = "claim"
+++
Documentation updates for bl-chore (gated on the implementation).

The spec is FROZEN (docs/architecture.md §0–§17), so this is a deliberate amendment, not drift — fix the doc to match the shipped capability (CLAUDE.md: don't implement a deviation; fix the doc).

Edits:
- docs/architecture.md §6: register bl-chore as a shipped default capability (alongside tracker/bl-delivery) and note it is opt-in — NOT in the default [hooks] schedule. Reference the design doc's enumerated touch-point lines.
- docs/architecture.md §11 (or §10): record the guarded-mint-at-claim primitive framing — that forge's hand-rolled claim.post mint and any build gate are bl-chore (create) + their own resolution; the recursion/idempotency guards have one home.
- SKILL.md / `bl skill`: add bl-chore to the Plugins section (what it is, the agentic create-side-not-enforcement point, how to opt in: `bl conf prepend claim.post bl-chore` + config).
- README.md: mention if it advertises shipped plugins.

Bake the WHY into the prose, not just the mechanism (philosophy in long-lived artifacts).