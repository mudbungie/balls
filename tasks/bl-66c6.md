+++
title = "Assert bl show's full human render"
created = 1784524138
updated = 1784524138
priority = 2
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]
+++
Integration-test coverage gap (audit bl-covtest, grade C-).

**Target file:** `tests/show_render.rs`

**Exercises:** show on: a fully-populated live ball (badge, tags, priority, blockers annotated on claim/on close, children rollup, claim-age line), a ball with -m journal notes, a closed id (retirement badge + retired date), an unknown id, and a hand-corrupted ball file.

**Assertion:** Each section's literal text appears (children with per-child badges, '(on close)' annotations, journal oldest-first after the body); unknown id yields 'no such ball: <id>'; the corrupt ball surfaces its parse error rather than a stale reconstruction.

**Priority:** med. Rules: touch ONLY tests/ (never src/ — keeps the 100% tarpaulin gate green); each .rs file <=300 lines; drive the real bl binary via assert_cmd.