+++
title = "Gate the close path on real project hooks and merge conflicts"
created = 1784524137
updated = 1784524137
priority = 3
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]
+++
Integration-test coverage gap (audit bl-covtest, grade C-).

**Target file:** `tests/delivery/gates.rs`

**Exercises:** Install an executable failing/passing pre-commit hook in the project fixture and close (bl-ee85); leave a MERGE_HEAD in the work worktree and close; advance integration with a conflicting change after claim and close (reintegration conflict, including a modify/delete).

**Assertion:** Failing hook aborts BEFORE the seal (task stays claimed, worktree stays up); passing hook delivers; MERGE_HEAD refusal names the bl-33db resurrection risk; the conflicting fold aborts loudly with the worktree left clean and no squash on main.

**Priority:** high. Rules: touch ONLY tests/ (never src/ — keeps the 100% tarpaulin gate green); each .rs file <=300 lines; drive the real bl binary via assert_cmd.