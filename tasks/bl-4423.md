+++
title = "Clean main of leak-scan findings, then wire make leak-scan into pre-commit and ci.yml"
created = 1789792351
updated = 1790143886
priority = 3
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"

[[blockers]]
id = "bl-816b"
on = "claim"
+++
bl-816b ported the scanner (scripts/leak-scan.sh, leak-rules.sh, leak-selftest.sh, leak-fixtures/, make leak-scan, store-scan.yml) but left the tree gate UNWIRED because main does not scan clean. Run `make leak-scan` for the current list. At port time (main 19054567), by rule:

home-path — real account paths in src/layout_tests.rs (4,18,19,27,34,37), src/encoding_tests.rs (10,11,47), src/delivery_path_tests.rs (46,48), docs/design/bl-8750-close-legibility.md (68,223-225), docs/e2e/bl-7911-greenfield-solo-lifecycle.md:40. Non-synthetic roots: a two-letter pronoun account name in delivery_path_tests.rs (9,10,23,27,39,40,41), an ellipsis-elided root in docs/demonstration.md (22,115), and the dot-dot traversal case in delivery_path_tests.rs:54. Remedy: the synthetic roots the rule excepts (u, op, x — both plain and percent-encoded spellings).

personal-email — the maintainer address in docs/architecture.md (246,528) and src/task_tests.rs (12,38): bl-325d CLOSED claiming it was replaced with the reserved documentation-domain address, but no delivery ever landed on main (git log main -S for that replacement in those files is empty). The one-letter test git identities (change_tests.rs:96, dispatch_test_support.rs:49, reads/test_support.rs:101) move to the reserved .test TLD. The SSH clone URLs (encoding_tests.rs 25,47; safegit.rs:237) are NOT addresses: a rule refinement is the honest fix — an EXCEPT entry for the git user at any host, with a clean.txt line — decide, do not just weaken.

Then: pre-commit step "leak scan" before clippy (sub-second); add scripts/leak-scan.sh + leak-rules.sh + leak-selftest.sh to the bl-speculate gate-file list (bl-6a84) — they are gate inputs; ci.yml step; make check dependency; drop the "not yet wired" sentences from README and the Makefile comment.

NOTE (2026-09-23): the first body of this ball quoted the findings verbatim, so store-scan.yml flagged this task file itself for four days running — the scanner reads the store, and a ball about a leak must describe it, never spell it. Rewritten to describe.