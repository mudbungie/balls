+++
title = "Release hygiene: .gitignore lcov.info; drop stale .balls from Cargo.toml exclude; delete wip/* tags; dedupe getrandom"
created = 1781036112
updated = 1781036112
parent = "bl-72a8"
priority = 2
+++
Four small pre-release items, one ball. (1) .gitignore is only target + *.profraw — lcov.info is NOT ignored, yet the coverage gate generates it in-tree; this is the source of the standing 'rm lcov.info before close' ritual. One line ends it. (2) Cargo.toml:16 exclude = [".balls", "scripts", "tests"] — .balls no longer exists anywhere post-XDG-move; drop the dead entry. (3) Two stale stash tags pollute tag listings next to release tags: wip/bl-7997-slithered, wip/bl-e802-stash — delete. (4) getrandom is duplicated 0.2.17 (direct, src/id.rs:65) vs 0.4.2 (via tempfile, dev-only); bump the direct dep to 0.4 (API: getrandom → fill, one call site) to dedupe — release binary is unaffected either way, so this leg is optional.

**Why:** (1) and (3) are recurring-friction killers; (2) is a stale fact in published crate metadata. All are one-liners best landed inside the release window so 0.5.0 ships clean.