+++
title = "Fold the pinned base SHA, not the ref name: sub-second pin-vs-fold disagreement window in deliver"
created = 1785124383
updated = 1785129632
claimant = "stevedore"
priority = -1
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
+++
Residual theoretical window noted while landing bl-8b89 (main 1005d6ac). Project::deliver now pins base = rev-parse(integration) before reintegrate, but reintegrate still folds the REF NAME; the two reads can disagree for a sub-second window. Forward moves are safe (the CAS on the pinned base rejects cleanly). The unsafe shape is contrived: integration RESET back to base after the fold read a newer tip — the CAS then delivers content integration no longer wants. Structural close: fold the pinned SHA instead of the ref name; cost is the delivery-conflict message naming a SHA instead of the branch name. Low priority — requires a mid-window ref reset, which no balls op performs.