+++
title = "SKILL.md asserts a nonexistent $BALLS_IDENTITY identity tier — align with the real chain (--as > $USER > unknown)"
created = 1781039367
updated = 1781039367
priority = 3
tags = ["docs"]
+++
Found during bl-4098: SKILL.md:83-86 documents identity resolution as --as > $BALLS_IDENTITY > $USER, but the binary reads no BALLS_IDENTITY env var (main.rs env reads: HOME/XDG/USER/BALLS_PLUGIN_DEPTH/NO_COLOR; verified in bl-4098, commit 2ab8197c, which fixed the same fiction in README.md:138's identity section). Re-verify with a repo-wide grep for BALLS_IDENTITY before editing (a plugin could read it), then either fix SKILL.md to the real chain (--as > $USER > "unknown" per src/edge.rs:62) or — if the env tier is actually wanted — implement it. Default: fix the doc; don't grow the surface. Note bl-56f7 also touched SKILL.md (warn gotcha line) — rebase on its landing.