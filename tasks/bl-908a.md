+++
title = "Drive the edge-authoring front door end-to-end"
created = 1784524135
updated = 1784524217
claimant = "Robber"
priority = 3
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]

[[blockers]]
id = "bl-6d00"
on = "close"
+++
Integration-test coverage gap (audit bl-covtest, grade C-).

**Target file:** `tests/edges.rs`

**Exercises:** Real-CLI `bl create --subtask-of E`, `bl create --blocks OP` and `--blocks ID:OP`, the bl-54fe write-time cycle refusal (create a close-gate, then `bl update <gate> --needs <parent>`), edge-target liveness refusals (--needs deadid vs closed id), and a generic non-claim/close gate (--blocks ID:update then attempt bl update).

**Assertion:** show --json carries the reciprocal edges (parent + on=claim gate for --subtask-of, on=close for --blocks); the cycle write exits nonzero naming the full cycle; liveness refusals distinguish 'not a known id' from 'already closed'; the gated bl update is refused via the real binary.

**Priority:** high. Rules: touch ONLY tests/ (never src/ — keeps the 100% tarpaulin gate green); each .rs file <=300 lines; drive the real bl binary via assert_cmd.