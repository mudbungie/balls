+++
title = "Docs §6: default hooks table — add the 'show' read-op wiring (post-bl-9ccb drift)"
created = 1781033484
updated = 1781033484
parent = "bl-72a8"
priority = 3
tags = ["docs"]
+++
Surfaced while implementing bl-9ccb (the bl-0af4 §11/§15 amendment). The code now seeds "show" = ["bl-delivery"] in default-config/plugins.toml and dispatches the bare "show" hooks key as a single "read" phase on the bl show HUMAN render (src/reads/readop.rs, Hooks::resolve_read) — §11 requires delivery answer show by default. But docs/architecture.md §6:
1. the default hooks table omits the "show" wiring;
2. the line "reads stay plugin-free in PRACTICE because nothing is listed for them by default" is now stale — the seed DOES list a read op by default.
Amend §6 (table + sentence) to match the landed behavior, referencing bl-0af4/bl-9ccb. Doc-only; follow the frozen-spec amendment convention (the normative change was already decided in bl-0af4 — this is conformance of the §6 prose to it).