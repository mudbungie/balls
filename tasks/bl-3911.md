+++
title = "Spec drift sweep: §6 hooks table vs shipped seed (3 rows), §6 prose, §12 seed-prune, §15 entries for --edit and --"
created = 1781033484
updated = 1781036141
parent = "bl-72a8"
priority = 3
tags = ["docs"]
+++
Widened 2026-06-09 from the original single 'show' row finding — the audit found the whole §6 normative example stale vs the shipped seed, plus adjacent freeze-discipline debts. The CODE is the conformant side throughout; this ball corrects the spec with tracked §15 entries (the doc's own header: 'a fresh entry in the §15 revision log — never a silent edit').

(1) §6 default hooks table (architecture.md:436-447) vs default-config/plugins.toml:16-28 — THREE rows: 'prime.post' = [bl-delivery] should be [bl-delivery, tracker] (§12:921 'prime/post settles the CONTENT… fetch-ff + push' requires the tracker there; bl-0a23's entry says it touched §12/§13 but the table was never updated); 'install.pre' = [tracker] is missing entirely (and §13's prime --install prose at :1124-1134 never says how the center's config is fetched — release-notes:88-89 does: 'The tracker — never core — does the remote fetch on prime --install, via an install.pre hook'); 'show' = [bl-delivery] is missing (the original finding). (2) §6:516-517 prose 'reads stay plugin-free in PRACTICE only because nothing is listed for them by default' — false since the show wiring shipped. (3) §12:898-899 specs seeding as a pure copy of default-config/, but src/seed.rs:46-58 PRUNES hook entries whose binary is absent before committing — defensible (test/tracker-less boxes) but unspecced; a §12 reader can't predict the committed landing varies by binaries present at first prime. Also note §6:453-454's dangling-bin 'clean error' coexists with the prune — spec should say both. (4) §9's exhaustive create/update flag enumeration (:643-679) and §15 lack entries for update --edit (bl-e196, f5a92250) and the -- end-of-options separator (bl-d31f, 087ac06f) — both shipped, both silent. (5) Cosmetic, same pass: spec spells 'dep tree' (:609), code token is dep-tree (src/verb.rs:75); §15:1566 (bl-7d46 'NN- prefix is already the structural lever') and :1601-1602 (recursive prime --install) lack SUPERSEDED markers though later entries retired both.

**Why:** the freeze makes architecture.md the single source of truth — every uncorrected row trains a reader on a wiring that isn't shipped. The drift all flows one direction (code moved, spec didn't), so the fix is pure documentation with §15 entries, no code risk.