+++
title = "Modernize balls-github-plugin to the current balls plugin contract"
created = 1781835042
updated = 1781835063
claimant = "Admonitory"
tags = ["gh-plugin"]
+++
The satellite plugin diverged from balls core's plugin contract after its 2026-06-09 rewrite. Decisive break: bl-5d9a (2026-06-17) flipped 'bl create --subtask-of' to gate CLAIM not close, so the forge plugin's review gate (minted at claim.post via --subtask-of) now blocks an already-completed claim instead of the close it exists to guard — it must become 'bl create --parent X --blocks close' (mirroring bl-chore). Also: tracker->bl-tracker rename (bl-27bf) in docs/comments; issues schedule examples must prepend github-issues before the irreversible bl-tracker push; drop the vestigial BALLS_BL/BALLS_BIN env reads (core sets only BALLS_PROTOCOL/NAME/DEPTH); standardize the forge committed-config path onto the <name>/config.json subdir (matching issues + bl-chore); add a tracked docs/design/forge.md. Tracked via the center/satellite federation.