+++
title = "prime warns when founding under an existing ancestor store"
created = 1784613767
updated = 1784691344
claimant = "Betides-Prime"
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"

[[blockers]]
id = "bl-c620"
on = "claim"
+++
Converged 2026-07-20 (dialogue on bl-0bd8): no automatic ancestor resolution — reads/mutations stay literal-cwd; the walk was solving a speculative problem. The ONE guard: bl prime, when founding at a cwd that has no store, first checks ancestors for a founded store — stat the percent-encoded clone dir per ancestor path, balls own record, git never consulted — and if one exists, warns on stderr before founding anyway:

  founding a new store here; an existing store sits at <ancestor> — meant that one? (cd there, or bl -C <ancestor>)

Report-only scrutiny, zero mechanism added to resolution: never refuses, never redirects, a founded cwd re-primes silently as today, non-prime verbs untouched. This demotes the invisible-sibling-substrate footgun (bl-0bd8, pinned in tests/invocation_scope.rs) from silent split to warned deliberate act — founding nested/sibling stores stays fully supported (a sane use case).

Needs bl-c620 first: the hint names the -C flag. Coverage: ancestor-scan helper unit-tested in src (100% gate; tarpaulin ignores tests/); e2e extends tests/invocation_scope.rs — subdir prime under a primed project now carries the warning, and the warning names the right ancestor. Docs: one line in skill/prime.md.