+++
title = "Tier 1 — collapse drift to single-source (dedup)"
created = 1781588979
updated = 1781589274
claimant = "Sicknesses"
parent = "bl-fac7"
+++
Five independent dedups, each two-representations-of-one-fact:
- --remote/--center parsing copy-pasted in checkout_args.rs (x2), mutate_args.rs, import.rs → one shared helper.
- finalize title-render byte-identical in change.rs:106,174,206 (Create/Occupancy/Update) → one finalize_titled() helper.
- status word↔parse: record.rs:11 status_word and flags.rs:83 apply_status are inverse hardcoded tables → single (word,Status) table, parse derived from it.
- git Command preamble hand-rolled 3x in delivery (run/ok in delivery_repo.rs, contained in delivery_standing.rs) → one git(cwd)->Command builder.
- plugins.toml path built 3x in hooks.rs:75,78,97 → one accessor.