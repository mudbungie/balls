+++
title = "remote_ops sync ff-only still surfaces raw git: give the remote non-ff the bl-fa89 voice"
created = 1785124847
updated = 1785125282
claimant = "lastage"
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"

[[blockers]]
id = "bl-d257"
on = "close"
+++
Found while landing bl-fa89 (which gave the store seal's ff-only loss a balls sentence): src/tracker/remote_ops.rs sync's ff-only still surfaces raw git ('git merge --ff-only: ...') on contention — the same legibility gap, one layer out at the remote. tests/claim_race.rs and docs/e2e/bl-1b49 both treat a non-ff there as the contention signal, so it has a voice to gain and existing coverage to rewrite against. Same shape as bl-fa89: wording plus tests, no retry mechanism; follow the commit_swap/seal precedent for phrasing (name what moved, state nothing was written if true, name the re-run).