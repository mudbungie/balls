+++
title = "Implement bl-chore plugin (binary, two guards, sample config)"
created = 1781809849
updated = 1781826804
claimant = "Soaks"
parent = "bl-6ee9"
priority = 2

[[blockers]]
id = "bl-3df3"
on = "claim"
+++
Implement the `bl-chore` plugin per docs/design/bl-chore.md (gated on the design doc landing).

Scope:
- The plugin binary: `protocol` self-description (op `claim`, phase `post`); on `claim.post` read the §7 payload, apply the two guards, render the configured `bl` commands with the injected tag + `--blocks close <claimed-id>`, shell them out. Honor `rollback claim.post` per the design.
- The two guards: tag-skip (always) + epic-skip (knob, default on, in the plugin's own config).
- Sample config shipped with the binary: the two base cases ("Run the test suite", "Review and update the docs") + the comment noting the epic-skip surprise.
- First-party, in the core repo. NOT added to the default `[hooks]` schedule (opt-in only).
- 100% coverage, 300-line cap per file (decompose to siblings if near).

Do NOT write the integration test or the doc updates here — those are sibling children. This child is the binary + unit coverage + sample config.