+++
title = "E2E regression test: full bl workflow in a non-git dir (bl-4a88 follow-up)"
created = 1782585693
updated = 1782585693
priority = 2
tags = ["test"]
+++
bl-4a88 fixed the non-git-dir delivery precondition (prime warns; claim/close abort cleanly), but the permanent tests drive the bl-delivery plugin binary directly. The bug was reported as the FULL bl workflow in a non-git dir, and only a one-off manual repro exercised that path.

Add a coverage-neutral (tests/) end-to-end regression test that runs the REAL bl binary through the exact reported repro in an un-git-inited tmp dir: prime --stealth (warns, exit 0), create (ok), claim (clean abort, NOT a raw 'fatal: not a git repository'), close (clean abort). This guards the wiring + relay layer the plugin-level tests cannot reach (default schedule wires bl-delivery, balls relays its warning, the abort surfaces as a nonzero bl exit).

Harness: mirror tests/chore_e2e.rs (isolated HOME/XDG, cargo bin dir on PATH, scrub BALLS_PLUGIN_DEPTH/NAME), minus the plugin-binding dance — bl-delivery is a default plugin bound by prime.