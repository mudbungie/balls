+++
title = "Pre-0.5.0 docs sweep: README + release-notes assert deleted behavior (stdout, delivery-worktree, install wiring)"
created = 1781036049
updated = 1781038748
claimant = "Redress"
parent = "bl-72a8"
priority = 1
+++
README.md and docs/release-notes-greenfield.md predate bl-0af4/bl-9ccb/bl-f387 and now assert the OPPOSITE of shipped behavior. These ship on crates.io as the 0.5.0 face, and the release-notes 'Compatibility & behavior notes' section exists specifically to be scripted against — so this is a release blocker.

Fix: (1) README:203 + :111 — 'its stdout is discarded (there is no return channel, §7)… bl claim does not echo the worktree path… you find it with git worktree list' — false: plugin stdout is Stdio::inherit (src/plugin.rs:211) and bl-delivery prints the path on claim.post/prime.post. (2) README:192 hooks example 'claim.pre = [bl-delivery] # stage the worktree-path field before the seal' — bl-0af4 deleted both the wiring and the stored field; show the shipped default-config/plugins.toml instead. (3) release-notes:135-141 — 'create is the only stdout-printing verb' + 'the path is also recorded in the task's stored delivery-worktree frontmatter key… not surfaced there yet' — the field no longer exists; delete, don't defer. (4) README:116 + release-notes:167 — 'Not yet wired standalone' for bl install — bl-f387 (4a46bd33) wired it fully. (5) release-notes:63 'prime --install recursively fuses' vs arch §13:1127 'It is a SINGLE hop, not a walk'. Sweep alongside: README:105 prime row ('Sync + show ready/claimed' — prime prints no listing), README:138 identity 'unknown' tier (no such tier), README:28/:185 adjacency-is-wiring garble (dispatch resolves config/plugins/bin/<name>, not adjacency), document prime --center.

**Why:** docs that contradict the binary are worse than no docs — an integrator scripting against 'create is the only stdout-printing verb' or the deleted delivery-worktree field builds on a fiction. Single source of truth: the spec + code agree; these derivative docs must re-derive, not drift.