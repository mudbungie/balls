+++
title = "Extend mutate flag coverage in the update suite"
created = 1784524140
updated = 1784524140
priority = 2
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]
+++
Integration-test coverage gap (audit bl-covtest, grade C-).

**Target file:** `tests/update.rs`

**Exercises:** `bl create -t a -t b`; `bl update --no-tag a` and `--no-priority`; `bl update --edit` with EDITOR pointed at a scripted fake editor.

**Assertion:** show --json reflects both tags, then the surgical tag removal and priority clear; the --edit run applies exactly the editor-written diff to the ball.

**Priority:** med. Rules: touch ONLY tests/ (never src/ — keeps the 100% tarpaulin gate green); each .rs file <=300 lines; drive the real bl binary via assert_cmd.