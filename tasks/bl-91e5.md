+++
title = "gh-plugin: build/infra parity (CI, pre-commit env scrub, unused deps)"
created = 1780996136
updated = 1780996692
claimant = "Scab"
parent = "bl-10a4"
priority = 2
tags = ["gh-plugin"]
delivery-worktree = "/home/mark/.local/state/balls/plugins/bl-delivery/home/mark/dev/balls-github-plugin/bl-91e5"
+++
(1) Add .github/workflows/ci.yml running the make-check gate (test + clippy + line-length + 100% coverage) on push/PR — README currently claims CI exists but there is none. (2) pre-commit: add 'unset $(git rev-parse --local-env-vars)' after cd ROOT (both plugins have git-spawning tests; GIT_DIR/GIT_INDEX_FILE leak retargets this repo's index). (3) Remove unused deps: chrono (workspace + shared, zero uses), reqwest from the issues crate (all HTTP via shared::http). (4) Rename COVERAGE_THRESHOLD→BALLS_COVERAGE_THRESHOLD for parity; fix stale 'A2 introduces' workspace Cargo.toml comment.