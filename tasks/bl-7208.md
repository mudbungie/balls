+++
title = "Lifecycle verbs on the wrong ball: closed, unclaimed, never-claimed, anonymous"
created = 1784525387
updated = 1784525401
claimant = "Revises"
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]

[[blockers]]
id = "bl-3360"
on = "close"
+++
PROBE (UX). Cheap real-CLI probes of the absence-is-the-record edges: (1) bl claim <closed-id> — known to error "No such file or directory" (a raw missing-file read); pin the CURRENT message and judge it: a closed task should be refused in balls voice ("already closed"/"no such ball"), not a filesystem errno — if the message is raw, that is a small bug to surface. (2) bl close <never-claimed-id> via the real CLI (plugin no-op is tested; the full dispatch path is not). (3) bl unclaim <never-claimed-id>. (4) A verb with --as omitted entirely: the documented $USER-then-"unknown" fallback stamps, and two defaulted agents colliding still get a coherent occupancy refusal. Extend tests/guards.rs (300-line cap — lift a sibling if needed).