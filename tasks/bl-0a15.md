+++
title = "Fleet reads and the machine JSON contracts"
created = 1784525384
updated = 1784525397
claimant = "Revises"
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]

[[blockers]]
id = "bl-a326"
on = "close"
+++
The read-surface promises agents script against, none pinned: (1) bl show <id> resolves a FOREIGN-root ball from another repo in the fleet (docs: "show stays global — naming an id is the signal"; currently correct only by omission). (2) list --everywhere --json: foreign rows present, render-only labels NEVER leak into the JSON. (3) A claimed row in list --json carries stored frontmatter only — no derived claim-age key. (4) list -s closed --json / --all --json: the reconstructed dead-row object round-trips id/claimant/tags/timestamps. (5) --since/--until against the closed set (dead rows filter on retirement date — a distinct code path, untested). (6) -s claimed --claimant NAME composes. (7) Priority actually orders list --json output ascending with no-priority last, re-sorted live after update -p/--no-priority. Extend tests/fleet.rs + tests/reads_list.rs (this ball owns BOTH files; 300-line cap — lift siblings if needed).