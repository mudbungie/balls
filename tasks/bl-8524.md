+++
title = "Cover the remote-ladder edge diagnostics and prime flag matrix"
created = 1784524140
updated = 1784524224
claimant = "Robber"
priority = 2
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]

[[blockers]]
id = "bl-74f6"
on = "close"
+++
Integration-test coverage gap (audit bl-covtest, grade C-).

**Target file:** `tests/remote_ladder.rs`

**Exercises:** Bare `--remote <url>` on a mutating op then a plain follow-up op; prime with ephemeral --remote (W2 warning); a center whose config names a different tasks branch (store_elsewhere warning); `prime --center+--install` and `--stealth+--center/--install` contradictions; `prime --as alice` founding-commit actor; `bl sync <branch>` positional; `bl doctor` unknown-verb; assert --install leaves binding.toml absent.

**Assertion:** The override pushes to the given remote once and the next op reverts to the ladder with no durable file written; each warning's literal stderr text appears; each contradiction is refused at parse; the founding commit records alice; the positional branch is pulled; doctor exits as usage error; no binding after --install.

**Priority:** med. Rules: touch ONLY tests/ (never src/ — keeps the 100% tarpaulin gate green); each .rs file <=300 lines; drive the real bl binary via assert_cmd.