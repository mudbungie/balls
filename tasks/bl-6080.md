+++
title = "The extras seam, the -m-only journal note, and import robustness"
created = 1784525383
updated = 1784525396
claimant = "Revises"
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]
+++
Three authoring promises never driven through the binary: (1) key=value preserved-extras — bl update <id> jira=PROJ-42 round-trips through show --json, bare jira= clears it, a RESERVED key (id=/created=) is refused by name; an imported record carrying an extra key preserves it verbatim. (2) The -m-only zero-field update (THE agent progress-note gesture, bl-cf93): always commits, renders in the journal oldest-first; two notes pinned to the same second via BALLS_CLOCK — the second aborts LOUDLY rather than silently dropping a note (the documented loud-loss guard; no tests/*.rs uses the BALLS_CLOCK seam today). (3) import stdin grammar: not-json, missing-id, non-object records each refuse cleanly naming the record, store untouched, never a panic. New file tests/extras_journal.rs (+ small import_roundtrip.rs extension only if cleaner).