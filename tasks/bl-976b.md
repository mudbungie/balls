+++
title = "Docs: §12/§15 — implicit origin discovery is the tracker's, from the project repo (not core/landing)"
created = 1780964906
updated = 1780964906
parent = "bl-72a8"
priority = 2
tags = ["greenfield"]
+++
Spec correction surfaced by bl-a476: §12 must state explicitly that implicit `origin` discovery (the bottom of the `--remote` > `--center` > XDG > `origin` precedence) reads the PROJECT repo (the invocation path) and is the TRACKER's job — never read off the landing, which is local-only (§2 install-transport) and carries no origin. Add the clarification to §12 and a §15 RESOLVED entry pointing at bl-a476 for the code follow-up. Docs-only (architecture.md); the checkout.rs code-doc belongs to bl-a476.