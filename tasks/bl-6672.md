+++
title = "Spec drift sweep #2: propagate logged §15 decisions the body missed (stdout channel, unclaim, header authority, hook dirs, union, E6, prime's branch prune)"
created = 1781043809
updated = 1781043809
parent = "bl-72a8"
priority = 3
+++
Doc-only, bl-3911-style: every item re-derives the spec from the build or propagates an already-logged §15 decision — no design content.

1. §7 "stdout is diagnostics" → contradicts §6's channel split (stdout = user-facing product forwarded verbatim; stderr = diagnostics) that bl-0af4's case (c) leans on. Propagation miss from bl-0af4.
2. §8 task-op family reads (`create`/`claim`/`update`/`close`) but says "fully symmetric across the five verbs" — add `unclaim` (omitted since drafting; with drop gone (bl-65e0) the count is five again).
3. REVISED-2026-06-05 header still says "Landing config is the sole authority" — bl-7d46(2) corrected §0/§4 to "the LOCAL stack (landing ⊕ XDG ⊕ CLI); no remote is ever authoritative" but missed the header.
4. §9 "Read verbs (… hook dirs only, §13)" — registry-era vocabulary; bl-8540 moved the schedule to the [hooks] config list. Same phrase survives in src/verb.rs's enum comment (comment-only code edit).
5. §0 "sync-merged (union, ff-only)" + §13 "fast-forwards/unions" — "union" is vestigial: remote_ops::sync is strictly fetch + ff-only, a non-ff aborts; no union path exists.
6. §12 error catalog jumps E5→E7 with no note for E6, while E2/E3/N3 each carry a retirement note. Dig the history for what E6 was; add the note (never renumber — codes are stable).
7. §12/§13 prime job list omits the settled-work/<id> branch prune that prime performs (skill doc: "prunes the settled work/<id> branches that delivered closes leave behind; a branch carrying committed undelivered work is kept") — §11 defers that cleanup to prime, but prime's own §§ never claim the job. Add it.

Out of scope (deliberate): the unnumbered "§ id generation" stays unnumbered (renumbering churns every cross-ref for zero meaning); §16's force-rewrite note stays (historical, cutover done); §5/§8's `config` op token vs bl-c2de's `conf` converges in that ball, not here.