+++
title = "gh-plugin: dedup wire/Binding + metadata_id into balls-github-shared"
created = 1780996139
updated = 1780996857
claimant = "Scab"
parent = "bl-10a4"
priority = 3
tags = ["gh-plugin"]
delivery-worktree = "/home/mark/.local/state/balls/plugins/bl-delivery/home/mark/dev/balls-github-plugin/bl-9090"
+++
Both plugins independently define a Binding over the same §7 fields and the 'pull sealed bl-id out of metadata' logic (forge resolve_id, issues Payload::id). Lift a shared Binding (superset: invocation_path + optional landing/store) and metadata_id(&Metadata)->Option<&str> into balls-github-shared; each plugin's Wire/Payload composes it, keeping op-specific fields local. Respect the projection boundary — generic protocol fields only, no external.<name>.*.