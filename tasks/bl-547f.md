+++
title = "Pave the half-close: kill cosmetic retry error + sharpen hint + lock direction (bl-c3c0 fix 3)"
created = 1782631096
updated = 1782631096
priority = 3
tags = ["core"]
+++
Per docs/design/bl-c3c0-lagging-clone.md (Fix 3). The 'half-close' (squash landed on main + task still claimed after a rejected close.post archival push) is the CORRECT failure direction — keep it; do NOT pre-reconcile or auto-sync (a common paved path beats a rare edge case). Pave it: (1) make deliver-from-missing-worktree a clean no-op so the retry close stops emitting 'bl: No such file or directory (os error 2)' (the squash already converges, bl-430e); (2) sharpen the push-rejection message so the 'bl sync + retry close' recovery is unmistakable; (3) add a test locking the direction (rejected close.post push => delivered+open, never done+leftover) guarding the teardown-before-push order in close.post = [bl-delivery, bl-tracker].