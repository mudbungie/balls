+++
title = "GH Actions remote builder"
created = 1786515684
updated = 1786515684
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["merge-queue"]

[[blockers]]
id = "bl-1263"
on = "claim"
+++
Remote writer of verdict records (design: docs/design/bl-24e7-speculative-merge-queue.md, open Q4). Public-repo standard runners are free with no minutes quota (real caps: 20 concurrent jobs on Free plan, 6h/job, fair-use backstop — merge-queue CI for the project itself is the intended use). Work: candidate delivery without publishing ref debris (throwaway namespace + TTL sweep, or push the computed tree only); fingerprint the HOSTED toolchain so remote verdicts key correctly; actions/cache for cargo to soften cold starts. Constraint carried from the design: a remote builder must never become a close dependency — cache miss already degrades to local build, keep it that way. WHY: because the verdict record is the whole interface, this ball is pure policy — a second writer, deletable without trace.