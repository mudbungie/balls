+++
title = "Reconcile never clobbers a dirty root checkout; the delivery gate honors core.hooksPath"
created = 1784525382
updated = 1784525382
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]
+++
Two documented safety properties with zero e2e proof: (1) src/delivery_reconcile.rs promises "refuse on dirty, never clobber" — dirty the root checkout with genuine uncommitted edits, land a sibling close from a worktree, assert the edits survive reconcile untouched (a loosened phantom-predicate silently discards user work). (2) src/delivery_repo.rs::gate resolves the hook via git rev-parse --git-path specifically to honor core.hooksPath — configure a custom hooks dir, assert ITS pre-commit gates the close (both fail and pass directions). Extend tests/delivery/ (reconcile.rs + gates.rs; watch the shared main.rs mod block and the 300-line cap).