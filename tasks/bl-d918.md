+++
title = "Coverage-honesty audit of the sibling forge/github-issues plugin repo"
created = 1781036066
updated = 1781040419
claimant = "Redress"
parent = "bl-72a8"
priority = 2
tags = ["review"]
+++
## Why

The bl-ae0b coverage-honesty audit was core-only. Forge-gated delivery
(`integrate.mode=forge-pr`, the gate-child lifecycle) is NOT in core — commit
2c679c4 "[bl-1280]" is an empty marker commit; that work delivered into the
sibling plugin repo (`/home/mark/dev/balls-github-plugin`). So this repo's 100%
gate gives **zero signal** on a full delivery variant. The audit can only certify
what its gate measures; that variant is currently certified by nothing here.

## Scope

Run the same coverage-honesty pass against `/home/mark/dev/balls-github-plugin`
(cross-repo: ball here, fix there). Confirm: a real coverage gate exists and is
honest; the forge-PR open/poll/merge paths and the gate-child auto-create +
close-on-merge are tested, not just line-covered; integration tests exec the built
plugin against throwaway repos; and the `[bl-xxxx]`-marker join SSOT (federation,
per bl-2a81) has tests. Deliverable: findings + tests added for the gaps in that
repo.