+++
title = "Prove cross-repo fleet scoping end-to-end"
created = 1784524137
updated = 1784524220
claimant = "Robber"
priority = 3
root_commit = "91c6469b14fef602e0bb5ab9957b09937623a0da"
tags = ["bl-covtest"]
+++
Integration-test coverage gap (audit bl-covtest, grade C-).

**Target file:** `tests/fleet.rs`

**Exercises:** Two distinct-root project repos enrolled on one shared center: plain `bl list` in each, `bl list --everywhere`, a wrong-repo `bl claim`, and the same flows from a project path containing spaces/unicode (percent-encoding round trip).

**Assertion:** Plain list hides foreign-rooted balls (rootless admitted); --everywhere shows all with '[project]' labels on foreign rows decoded from the clone dir name; the cross-root claim is refused; all ops resolve the encoded clone dir idempotently.

**Priority:** high. Rules: touch ONLY tests/ (never src/ — keeps the 100% tarpaulin gate green); each .rs file <=300 lines; drive the real bl binary via assert_cmd.