{
  "branch": null,
  "claimed_by": null,
  "closed_at": null,
  "closed_children": [],
  "created_at": "2026-05-29T19:43:47.110418952Z",
  "delivered_in": null,
  "depends_on": [],
  "description": "# balls — greenfield architecture (working document)\n\nUpdated as topics close. Sibling topic tasks track open questions.\n\n## §0 Overview\n\nballs is a task tracker. State lives entirely on one branch (default `balls`) of a git repo. All persistence is git, local-first. The `tracker` plugin (shipped with balls) is the only thing that talks to a remote. Other plugins are user-supplied binaries invoked in sequence on every state change. Federation is a tracker config: point at someone else's balls branch and you shadow theirs underneath yours.\n\nBase balls is the smallest possible thing: it knows how to find/create a local git repo at `operating/` and commit to its `balls` branch. Everything that connects to the world is a plugin. Stealth mode is simply \"no tracker plugin\" — base balls falls back to its own local repo.\n\n## §1 XDG layout\n\n```\n$XDG_CONFIG_HOME/balls/\n  config.yaml                                      # user-level config\n\n$XDG_STATE_HOME/balls/\n  plugins/                                         # each plugin owns one subtree\n    tracker/<url-encoded-remote>/                  # clone of the balls branch\n      .git/  tasks/  config/\n      change-attempts/<uuid>/                      # in-flight worktrees\n    <other>/<plugin-territory>/\n\n  clones/<url-encoded-local-path>/                 # per-invocation-path binding\n    binding.yaml                                   # which tracker remote (if any)\n    operating/                                     # the git repo balls operates against\n                                                   # tracker enabled: symlink → plugins/tracker/<remote>/\n                                                   # tracker disabled: a real dir\n    logs/<plugin>/plugin.log                       # suggested; plugins do as they please\n```\n\n- URLs and paths are percent-encoded (no hashing) so dir names stay inspectable.\n- Tracker owns its own clone; it does NOT share refs with the user's working repo. Two clones with the same remote URL share one tracker dir.\n- Change-attempt worktrees are git worktrees of the tracker clone.\n\n## §2 Branch layout (committed to `balls`)\n\n```\ntasks/<ball-id>.md                # one file per task\nconfig/balls.yaml                 # federated balls config\nconfig/plugins/<name>/...         # each plugin's federated config; balls never reads it\n```\n\nNo archive dir. Closed/dropped balls delete their file — history lives in `git log`.\n\n## §3 Ball schema\n\n`tasks/<id>.md`:\n\n```markdown\n---\nid: bl-1234\ntitle: Refactor the foo system\nstate: open\ncreated: 2026-05-27T14:32:00Z\nupdated: 2026-05-28T09:15:00Z\nclaimant: orionriver@gmail.com\nparent: bl-1000\ndeps: [bl-1100, bl-1200]\ntags: [refactor, infra]\n---\n\n# Description\n\nFree-form markdown.\n```\n\n- `id`: string-safe; default scheme `bl-XXXX`; configurable.\n- `state`: `open | claimed | review`. Extensible via config (§see states-as-config topic).\n- `created` / `updated`: RFC3339 UTC.\n- `claimant`, `parent`, `deps`, `tags`: optional.\n- Unknown frontmatter keys are preserved on writeback.\n- Terminal states (`close`, `drop`) DELETE the file; recovery is via `git log` on the balls branch.\n\n## §4 Config schema\n\n**Layers, innermost wins:**\n1. CLI flags\n2. `$XDG_CONFIG_HOME/balls/config.yaml`\n3. `config/balls.yaml` on the satellite balls branch\n4. `config/balls.yaml` on the center balls branch (only if `config/plugins/tracker/remote.yaml` points at one; one hop)\n5. Built-in defaults\n\n**Merge semantics:**\n- Scalar/object fields: innermost layer fully replaces outer.\n- List fields: bare `<field>` = full replacement. Compose with `<field>_prepend`, `<field>_append`, `<field>_ban`. No `_override` (bare assignment already replaces).\n\n**Built-in fields (initial set):**\n\n- `branch` (string, default `\"balls\"`)\n- `id_scheme` (object, default `{ prefix: \"bl-\", length: 4 }`)\n- `plugins` (list of `{name, path}`, default `[{name: tracker, path: balls-tracker}]`)\n- `default_state` (string, default `\"open\"`)\n- `states` (list, default `[open, claimed, review]`) — terminals (`close`, `drop`) are ops, not states\n\n## §5 Commit message protocol\n\nEvery change-attempt commit:\n\n```\n<subject — free-form, human-readable>\n\nop: close\nball: bl-1234\nfrom-state: claimed\nto-state: closed\nactor: orionriver@gmail.com\n\n<free-form body>\n```\n\nSections are blank-line separated. The k:v block is parsed by balls and forwarded to plugins in the payload. Subject and body are display.\n\n**Reserved keys** (balls writes these): `op`, `ball`, `actor`, `from-state`, `to-state`, `protocol`. balls does not police them: a plugin that amends the commit can rewrite anything in the message and owns the consequences (§7).\n\n## §6 Plugin contract\n\nA plugin is a single binary. balls invokes it with a verb. Internal dispatcher: `bl plugin <name> <verb>` is the canonical entrypoint; balls dogfoods it.\n\n```\n<bin> protocol\n  → stdout (JSON): { version: 1, verbs: [\"invoke\",\"rollback\",\"init\",\"sync\"] }\n  → exit: 0\n\n<bin> <verb>\n  cwd:       change-attempt worktree (invoke/rollback) or operating/ (init/sync/prime)\n  env:       BALLS_PROTOCOL=1, BALLS_PLUGIN_NAME=<name>  (CLI flags can override)\n  stdin:     payload (JSON or YAML) — intent only; the change itself lives in the worktree\n  stdout:    diagnostics (advisory; balls does NOT parse it back into state)\n  stderr:    human log; streamed and captured to clones/<path>/logs/<name>/\n  exit:      0 = ok; non-zero = abort + rollback prior plugins in reverse\n```\n\nballs only calls verbs the plugin declares in `protocol` output. A plugin contributes to an op by **editing its cwd worktree** — ball files, the commit message, its own territory — not by returning data for balls to merge. There is one plugin phase, not two (§7, §8).\n\n## §7 Wire payloads & the no-return-channel rule\n\n**Philosophy:** a plugin gets meaning directly — content + intent. The *content* it reads and changes is the change-attempt worktree it runs in (its cwd). The *intent* is a small payload on stdin. Hashes appear when relevant (tracker uses them) but are reference, not load-bearing.\n\n**There is no return channel.** A plugin changes an op by writing the worktree, not by handing balls structured output to merge. This is the settled answer to the pre-invoke-return-channel topic, and it dissolves the field-conflict question with it:\n\n- No `field_overrides` / `metadata_additions` stdout protocol. A plugin that wants to set a field edits the ball file; one that wants to annotate the record `git commit --amend`s the message.\n- **No conflict policy.** Two plugins touching the same field are just two writes to a file — **last writer wins**, by the filesystem. balls does not detect, arbitrate, or track ownership. Plugins coordinate (or don't) among themselves.\n- **No `id` special-casing.** `id` is an ordinary field. A plugin that assigns one (e.g. jira → `PROJ-1234`) rewrites the frontmatter and `git mv`s the file. There is no `id_authority` key and no immutability guard; a plugin that rewrites an id in flight owns the result.\n- **Reserved k:v keys are not defended.** balls writes the §5 block; a plugin may amend it. balls does not protect its own keys — symmetric with everything else.\n\nComposition is automatic: plugin N+1 sees plugin N's edits because it is the *same worktree*, not because balls replayed anything.\n\n**invoke payload (stdin) — intent only:**\n\n```yaml\nprotocol: 1\nverb: invoke\nplugin_name: jira\nactor: orionriver@gmail.com\nbinding: { remote: ..., branch: ..., operating: ... }\n\nop: create               # create | claim | unclaim | review | close | drop | update | config\nball: bl-1234            # balls' id for the ball; a plugin may reassign by editing + renaming the file\nfrom_state: null         # null on create\nto_state: open\ncommit: 9bf21e...        # the staged change-attempt commit — reference; amend in place to alter the record\n```\n\nThe worktree at cwd already holds the staged change (balls' file write, committed with the §5 message). The plugin reads current and prior ball content from the tree and `git log`; it changes the op by editing files, amending, or adding commits. balls re-reads the tree after the chain to learn the final state — **the worktree is the source of truth, never anything the plugin prints.**\n\n**rollback payload:** same shape, plus `rolling_back: true`. In-tree edits need no rollback — an aborted op discards the change-attempt worktree before it ever merges into `operating/`. Rollback exists only for plugins with *external* side effects (e.g. tracker already pushed a commit), which undo their own. Precise contract: see the rollback-semantics topic.\n\n## §8 Op lifecycle (state-change ops)\n\n```\n1. Snapshot plugin list.\n2. Create change-attempt worktree off `balls` HEAD; apply balls' file change;\n   commit it with the §5 structured message. (The worktree IS the staging area.)\n3. For each plugin in chain order: call invoke, cwd = worktree. The plugin edits\n   files, amends/adds commits, or writes its own territory.\n   Non-zero → discard worktree + rollback prior plugins in reverse (external\n   effects only) + abort.\n4. Re-read the worktree to learn the final ball state.\n5. Merge --no-ff into operating/. Done.\n```\n\nOne phase, not two. The change-attempt worktree is the staging area, so the old \"before commit\" (pre-invoke) / \"after commit\" (invoke) split collapses: a plugin **rejects** by exiting non-zero (the worktree is discarded, never reaching `operating/`), **augments** by editing the tree, and **reacts** by doing its external work — all in one call. Reads (`bl list`, `bl show`) never invoke plugins. The phase-collapse and the precise rollback contract are ratified by the dispatcher-contract and rollback-semantics topics.\n\n## §9 bl init lifecycle\n\n```\n1. Resolve handle (local_path, enc_local, clone_dir).\n2. Guard re-init (--reinit override).\n3. Resolve initial plugin list (CLI > XDG > defaults; no federation yet).\n4. Resolve tracker remote (only if tracker on chain). --remote > XDG > git remote get-url origin. If none and tracker is on the chain: hard fail.\n5. Materialize clone_dir; write binding.yaml.\n6. Run the plugin chain with verb=init. Tracker: clone the balls branch into plugins/tracker/<enc>/ (or scaffold orphan), symlink operating/ to it. Other plugins: their territory.\n7. Look at operating/. Valid git repo on `balls` branch? Yes → done. No → git init it, checkout -b balls, seed config + empty tasks/, commit.\n8. Reread config from federated layers, refresh binding.yaml if plugin list changed.\n```\n\nStep 7 is the converging predicate: tracker-symlinked, re-init, or fresh stealth all look identical to base balls.\n\n## Open topics (see sibling tasks)\n\nEach topic, when settled, becomes a new §here.\n\n- Plugin dispatcher contract (protocol verb, recursion, install + snapshot)\n- Commit message k:v parser rules\n- bl new walkthrough (create flow, plugin-assigned id)\n- State transitions (claim/unclaim/review) walkthrough\n- Terminal ops (close/drop) walkthrough\n- bl sync / bl prime walkthrough\n- tracker adopt / disown verbs\n- Federation: shadow config, center remote.yaml, one-hop\n- Pre/post hook rollback semantics\n- States as extensible config\n- ID generation scheme\n- bl doctor: drift checks, stale worktree cleanup\n- Migration path from current balls\n\n## Settled topics\n\n- **Pre-invoke return channel + field-conflict policy** (bl-4997) → §5/§6/§7/§8. Resolved by removal: there is no return channel, so there is no field-conflict policy. Plugins mutate an op by editing the change-attempt worktree; same-field double-writes are last-writer-wins via the filesystem; `id` is an ordinary field (no `id_authority`); reserved k:v keys are undefended; pre-invoke and invoke collapse to one phase.",
  "external": {"github-issues":{"issue":{"last_synced_status":"open","number":211,"source":"balls","state":"open","synced_at":"2026-05-31T19:52:53.908383278+00:00","url":"https://github.com/mudbungie/balls/issues/211"}}},
  "id": "bl-2e26",
  "links": [],
  "parent": "bl-b465",
  "priority": 2,
  "repo": "git@github.com:mudbungie/balls.git",
  "status": "open",
  "synced_at": {"github-issues":"2026-05-31T19:52:53.926699063Z"},
  "tags": [],
  "title": "Spec: greenfield architecture (working document)",
  "type": "task",
  "updated_at": "2026-05-31T19:52:53.926707278Z"
}
