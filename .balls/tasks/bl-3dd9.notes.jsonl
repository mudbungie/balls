{"ts":"2026-06-06T22:59:26.835323199Z","author":"Besting","text":"REVISED 2026-06-06 (path-copy supersedes scope-addressed): install is PURE PATH-COPY — `bl install <path>` makes dest/<path> == source/<path>, siblings untouched. NO Object enum, NO merge-vs-replace logic. Path SHAPE decides: folder = MIRROR (rsync --delete; deletions propagate — this is how closes carry, dissolving the resurrection worry by addressing); file/glob = UNION (overwrite on collision, NO conflict-detection, git recovers). Per-task install (install tasks/bl-1234.md) falls out free. Bare `bl install` (no path) = config/ EXCLUDING tasks/. Never copies bin/ (gitignored, not in tree). Drop the old union-merge conflict logic entirely. spec bl-2e26 §6."}
{"ts":"2026-06-07T19:52:41.575809103Z","author":"Unlearn","text":"AMENDED by bl-8540 (merged e9b44cc): the schedule is no longer a symlink tree under config/plugins/<op>/<phase>/ — it is config/plugins.toml (§6). So 'install plugins' wiping+copying the symlink wiring (copy_wiring + the stale-symlink prune this ball adds) LARGELY DISSOLVES: the schedule is an ordinary scoped file, so 'install plugins.toml' is a plain scoped replace under the same path-copy rule; 'install config' still replaces all of config/ (incl. plugins.toml + plugin config dirs). config/plugins/ now holds only per-plugin config (config/plugins/<name>/) + gitignored bin/. KEEP unchanged: committed-tree-only (never bin/), local binary resolution + protocol validation, the subtree-only-rewrite invariant."}
{"ts":"2026-06-08T00:15:32.656981694Z","author":"Scribble","text":"bl-4e14 LANDED (main 7fd733f) — partial overlap, read carefully. WHAT IT DID: symlink registry fully retired, so install.rs copy_wiring/walk/record_ref + the stale-symlink-prune this ball described are DELETED (no symlinks exist to mirror anymore — that worry is moot). Object::Plugins is now a plain file copy of config/plugins.toml with Referenced read via hooks::Hooks::referenced(); parse_entry gone. STILL OPEN (the real remaining scope of THIS ball): the PATH-COPY redesign is NOT done — install.rs still has the Object{Plugins,Config,Tasks} enum, transfer(), copy_config (balls.toml), and merge_tasks with union+InstallError::Conflict. The spec (Besting note) wants: drop the enum + all merge-vs-replace/conflict logic; `bl install <path>` = pure path-copy where SHAPE decides (folder=MIRROR rsync --delete, file/glob=UNION, no conflict detection); bare `bl install` = config/ EXCLUDING tasks/. Also: install is STILL not wired to a run verb (returns its op plan; git-free unit, tested on tempfiles). KEEP unchanged (already correct): committed-tree-only/never bin/, resolve_and_bind protocol validation, subtree-only-rewrite invariant."}
