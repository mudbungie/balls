{
  "branch": "work/bl-77cb",
  "claimed_by": "snuffled",
  "closed_at": null,
  "closed_children": [],
  "created_at": "2026-05-24T07:15:29.678311218Z",
  "delivered_in": null,
  "depends_on": [],
  "description": "**Status: shipped as the foundation slice of Phase 1 (bl-f021). The integration into `Store::discover` / `bl init` / `bl claim` was split off into a separate child ball; see the linked sibling.**\n\nFirst implementation slice of bl-f021. Lands the *foundation* of the nested-XDG layout (SPEC-clone-layout §3 / §4 / §6 post-bl-e9c2) — the seven pure modules every later integration slice rests on. No behavior change for existing clones: nothing is wired into `Store::discover`, `bl init`, or `bl claim` yet.\n\nThis ball was originally scoped to bundle foundation + integration into one coherent slice (the original ball description argued against splitting). SPEC-clone-layout was rewritten in bl-e9c2 to drop hashing entirely in favour of transparent percent-encoded paths and a nested per-clone tree, and to add a fourth config file (`clone.json`). Re-planning against that rewrite revealed the integration is a sweeping refactor of `Store::discover` and its callers (state-repo, init, claim, worktree, every conformance test) that warrants its own ball. The foundation alone is large enough and self-contained enough to ship.\n\n## Shipped in this ball\n\nSeven new modules, 100% line coverage, clippy clean, all under the 300-line cap:\n\n1. **`encoding`** — URL canonicalization + RFC 3986 percent-encoding + nested clone path. Pure functions. Frozen golden vectors per §14.2. Matches the §10 hand-operable shell sequence byte-for-byte. Constant `ENC_BALLS_TASKS` = `\"balls%2Ftasks\"`.\n\n2. **`xdg_paths`** — flat per-host layout under `~/.local/state/balls/{trackers,worktrees,claims,locks,plugins-auth}/` and `~/.config/balls/<nested>/`. `XdgBases::from_env` + pure `from_strings` for testability without env mutation (bl-bfa8). `tracker_checkout`, `own_tracker_checkout`, `PerClonePaths`, `clone_json_path` are the call surface.\n\n3. **`tracker_json`** — strict pointer-only redirect schema (§6.1 / §14.6). `state_url` required, `state_branch` optional. `deny_unknown_fields`. Presence-or-absence is the bootstrap signal (§5); `read_optional` returns `Ok(None)` on missing.\n\n4. **`layered_fields`** — shared field types for the layered config block: `Integrate`/`IntegrateMode` (replaces `Delivery`/`DeliveryMode` with `direct`/`forge-pr`), `ReviewBlock` (replaces `ReviewConfig` with `gate_command`). Legacy field-name values (`local-squash`, `deferred`, `pre_check`) are explicitly rejected per §14.16 / §14.17. The legacy types stay where they are.\n\n5. **`project_config`** (extended) — added the five SPEC §6.2 second-block optional layered defaults (`integrate`, `review`, `require_remote_on_*`). `Option<>`-typed so the precedence merger can distinguish \"explicitly set\" from \"absent — fall through.\" `skip_serializing_if = None` so existing project.json files round-trip byte-identically.\n\n6. **`repo_json`** — per-code-repo config (§6.3). Primary owner of layered fields plus four repo-only fields. Two read-time trip-wires per §6.5 / §6.7: tracker-scope fields (`version`/`id_length`/`min_bl_version`/`plugins`) abort the read; the pre-revision `target_branch` field aborts. Lenient on truly-unknown fields per §6.9. Hand-written `Default` impl returns SPEC built-ins (the derived Default's `bool::default()` would have given `false` for `auto_fetch_on_ready` and `protected_main` — caught by the merger tests).\n\n7. **`clone_json`** — per-on-disk-checkout override (§6.4). Owns `stealth`/`tasks_dir` (§4.1 retires the previous revision's \"synthetic origin-key\" fallback). Carries optional overrides for every layered field. Reuses `repo_json::check_forbidden_fields` for the same §6.5 trip-wires under its own file label.\n\n8. **`effective_config`** — three-layer precedence merger (§6.5 / §14.8). Returns one `EffectiveConfig` from `(project, repo, clone)`. The only seam the binary should consult for layered fields — reaching into individual layers directly bypasses the rule.\n\n## What's NOT in this ball (deferred to the integration ball)\n\n- `Store::discover` rewrite to use the new layout first / dual-read for legacy\n- `bl init` rewrite to write XDG-only (no on-main writes, no .gitignore entry)\n- `bl claim` worktree relocation to `~/.local/state/balls/worktrees/<nested>/<id>/`\n- Stealth wiring through `clone.json` (replacing `.balls/local/tasks_dir`)\n- Dual-read of pre-XDG (`.balls/config.json` on main) and bl-fc50 (hashed XDG) layouts\n- Conformance tests §14.1, §14.3, §14.4, §14.7, §14.10, §14.11, §14.12, §14.13, §14.15, §14.19, §14.20 (anything that requires the discover/init/claim seams to be on the new layout)\n\nConformance tests §14.2, §14.6, §14.8, §14.9 (encoding, tracker.json strict, three-way precedence, tracker-scope rejection) are *de facto* satisfied by the module-level tests on `encoding`, `tracker_json`, `effective_config`, `repo_json`/`clone_json`. The integration ball will add the end-to-end fixtures.\n\n## Why split now\n\nThe integration is genuinely a sweeping refactor — `Store::discover` is hot (every `bl` invocation runs it), every conformance test fixture assumes the legacy layout, and many call sites consult `store.balls_dir()` for in-repo paths. The bundling argument in the SPEC §15 paragraph (and in this ball's original description) holds for the original three-commit attempt, where most of the work *was* the integration. With the foundation modules built out as a separate, internally-coherent layer, the integration becomes a focused diff against a stable seam.\n\nThe integration ball can land as a single slice in its own right: it consumes the seven modules here as a black box, rewires `Store::discover` + `bl init` + `bl claim`, ports the conformance suite to the new layout, and ships dual-read for legacy clones.\n\n## Conformance tests gated by this ball\n\n§14.2 (golden URL encoding vectors), §14.6 (tracker.json strict), §14.8 (three-way precedence), §14.9 (tracker-scope field rejection), §14.16 / §14.17 (new field-name acceptance + legacy-name rejection on freshly-written files). All asserted via module-level tests on `encoding`, `tracker_json`, `effective_config`, `layered_fields`, `repo_json`, `clone_json`.",
  "external": {"github-issues":{}},
  "id": "bl-77cb",
  "links": [],
  "parent": "bl-f021",
  "priority": 2,
  "repo": "git@github.com:mudbungie/balls.git",
  "status": "in_progress",
  "synced_at": {"github-issues":"2026-05-25T17:14:23.266095796Z"},
  "tags": ["architecture","xdg"],
  "title": "Phase 1: dual-read XDG layout, bl init writes XDG only",
  "type": "task",
  "updated_at": "2026-05-25T18:40:47.864965132Z"
}
