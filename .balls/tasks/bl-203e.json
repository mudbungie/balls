{
  "branch": "work/bl-203e",
  "claimed_by": "mark",
  "closed_at": null,
  "closed_children": [],
  "created_at": "2026-05-25T18:41:36.538726345Z",
  "delivered_in": null,
  "depends_on": ["bl-77cb"],
  "description": "Second implementation slice of bl-f021 — the integration half of Phase 1. Builds on the foundation modules shipped by bl-77cb (`encoding`, `xdg_paths`, `tracker_json`, `layered_fields`, `repo_json`, `clone_json`, `effective_config`, plus the layered-default fields on `project_config`) and wires them into `Store::discover` / `bl init` / `bl claim`, plus the legacy-layout dual-read safety net.\n\nThis ball was split off bl-77cb after the foundation landed cleanly. SPEC-clone-layout's §15 calls bl-77cb's full slice \"the foundation + integration bundle\"; the split is a pragmatic choice on top of that — the integration is invasive enough to warrant a focused diff against a stable seam, and the foundation alone is shippable as a no-behavior-change layer.\n\n## Scope\n\nPer SPEC-clone-layout §3, §4, §5, §7, §8, §11 (read side), §12.\n\n### Implements\n\n1. **`Store::discover` rewrite.** New layout *first*, dual-read for legacy:\n   - Read `clone.json` at `~/.config/balls/<nested-clone-path>/clone.json`. On `stealth: true`, take `tasks_dir` as the task store and skip the entire origin/`trackers/`/redirect machinery (§4.1, §7 step 2, §14.15).\n   - Otherwise derive `<enc-origin>` from `origin.url`. Hard-fail on missing origin in non-stealth mode (§4).\n   - Materialize `~/.local/state/balls/trackers/<enc-origin>/<enc-balls-tasks>/` via single-branch fetch. Subsequent invocations fast-forward.\n   - Read `.balls/tracker.json` from that checkout. If present, follow the redirect (§5, single-hop only; chained redirects abort per §14.5).\n   - `mkdir -p` the per-clone tree under `worktrees/`, `claims/`, `locks/`, `plugins-auth/`.\n   - Load `project.json`/`repo.json`/`clone.json` and produce the `EffectiveConfig` (consumed by the rest of bl).\n\n2. **Dual-read for legacy layouts** (the safety net every later phase rests on):\n   - **Pre-XDG layout detection.** `.balls/config.json` on main + `.balls/state-repo/` runtime + `.balls-worktrees/` in tree. Read from the in-repo layout instead of XDG; warn one line `legacy layout in use; run \\`bl migrate\\` to relocate` per §12.\n   - **bl-fc50 hashed-XDG layout detection.** `~/.local/state/balls/<origin-key>/state-repos/own/.balls/` exists. (This layout never shipped, but defense-in-depth.)\n   - Apply legacy field renames in memory (`delivery` → `integrate`, `review.pre_check` → `review.gate_command`) without writing back.\n   - Preference rule: new (nested) > bl-fc50 (hashed) > pre-XDG. Phase 2 migrate may run mid-way and leave the old around briefly.\n\n3. **`bl init` rewrite — XDG only.**\n   - No `.balls/` at the clone root, no `.gitignore` entry, no `balls: initialize` commit on main.\n   - Pushes a `balls/tasks` orphan branch with default `repo.json` + `project.json`, no `tracker.json` (solo case).\n   - `bl init --stealth [--tasks-dir <path>]` writes `~/.config/balls/<nested-clone-path>/clone.json` with `stealth: true` + `tasks_dir`. No origin requirement; no `trackers/` subtree.\n   - §14.1, §14.19 (no on-main writes in steady state).\n\n4. **`bl claim` worktree relocation** (§8, §14.13).\n   - `git worktree add` against `~/.local/state/balls/worktrees/<nested-clone-path>/<bl-id>/`. On a legacy-detected clone, the legacy in-tree path is preserved (until Phase 2 migrate moves them).\n\n### Conformance tests\n\nSPEC-clone-layout §14 tests gated by this ball: 1, 3, 4, 5, 7, 10, 11, 12, 13, 14 (read-only \"moved clone\" detection — `--rebind-path` deferred to a follow-up), 15, 19, 20. Plus §14.16 / §14.17 *under the Phase-1 dual-read branch* (read old field names, warn, apply rename in memory).\n\nEach test must exist and fail for the right reason against pre-integration code before the ball lands. New-side assertions per SPEC-tracker-state §16.13 — no pinned old-binary fixtures.\n\n### Explicitly NOT in scope\n\n- `bl migrate` command itself — Phase 2 (bl-717e).\n- `bl prime --migrate` / auto-detect warning beyond the one-line legacy warning — Phase 3.\n- `min_bl_version` bump on the orphan tracker — Phase 4.\n- Cross-doc rename of SPEC-forge-gated-delivery §5 (the `delivery`/`target_branch` references) — lands with Phase 2.\n- `bl doctor` enrichment for moved-clone detection (§14.14) — follow-up; Phase 1 just needs to not regress doctor's current behaviour.\n- `--rebind-path` recovery action — follow-up; doctor only reports the orphan in Phase 1.\n\n## Risks\n\n- `Store::discover` is hot — every `bl` invocation runs it. The branch-on-layout logic must be O(1) on the common path; legacy-detection should short-circuit via a single stat (`clone.json` first, then `trackers/<enc-origin>/<enc-balls-tasks>/`).\n- Many existing tests assume the legacy in-repo layout. The integration will require parallel updates to `tests/conformance_*.rs` and the common test fixtures.\n- The `Store` struct currently exposes `balls_dir()`, `local_dir()`, `claims_dir()`, etc. — call sites consult these for in-repo paths. In the new layout, these need to return XDG paths. Either a layout-aware Store with mode-dispatched methods, or a layout enum the methods inspect — pick the one with the smaller blast radius and document why.\n- Nested clone paths can approach `PATH_MAX` on long usernames + deep project hierarchies. Document the limit; don't engineer around it (§13: no hashing for \"compactness\").\n\n## Why this ball, not three smaller ones\n\nThe discover rewrite, dual-read, init, and claim relocation all bind into the same `Store::discover` seam and its immediate callers. Splitting them produces intermediate states where (a) discover knows the new paths but init still writes the old, or (b) init writes the new but claim still uses the old worktree path. Both intermediate states would require keeping the legacy code path live as the actively-used one — defeating the migration's purpose for new clones.\n\nThe foundation/integration split made by closing bl-77cb early was a different cut: the foundation modules are *additions* to the codebase that don't touch existing code at all, so they ship as a no-behavior-change layer that the integration consumes wholesale.\n\n## Dependencies\n\nHard depends on the foundation modules from bl-77cb (already on main once that closes). No other prerequisites.",
  "external": {"github-issues":{"issue":{"last_synced_status":"open","number":151,"source":"balls","state":"open","synced_at":"2026-05-25T18:41:37.384335742+00:00","url":"https://github.com/mudbungie/balls/issues/151"}}},
  "id": "bl-203e",
  "links": [],
  "parent": "bl-f021",
  "priority": 2,
  "repo": "git@github.com:mudbungie/balls.git",
  "status": "in_progress",
  "synced_at": {"github-issues":"2026-05-25T18:41:37.410708771Z"},
  "tags": ["architecture","xdg"],
  "title": "Phase 1 integration: Store::discover + bl init + bl claim + dual-read",
  "type": "task",
  "updated_at": "2026-05-25T18:50:03.930721019Z"
}
