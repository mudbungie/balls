{
  "branch": "work/bl-3ce9",
  "claimed_by": "Gasses",
  "closed_at": null,
  "closed_children": [],
  "created_at": "2026-05-21T04:12:45.576746469Z",
  "delivered_in": "d70ab4acdd0776cb204756fa24d06775148b0003",
  "delivered_repo": "git@github.com:mudbungie/balls.git",
  "depends_on": [],
  "description": "# Why\n\nThe integration-test helpers that launch the `bl` binary — `tests/common::bl`, `tests/common::bl_as`, and the `bl_fresh` closure in `tests/init.rs` — each loop `GIT_ENV_VARS` and `env_remove` it from the `assert_cmd::Command` before running `bl`.\n\nThat scrub does nothing. It defends the wrong layer:\n\n- `bl` is already immune to an inherited `GIT_*` environment. It discovers the repo via `git rev-parse --show-toplevel` and runs every git subprocess through `git::clean_git_command`, all of which scrub `GIT_ENV_VARS` themselves (the production path, unchanged by bl-d1db). Scrubbing the env of an already-immune binary buys nothing.\n- And in practice `GIT_*` is not even set when the suite runs: the pre-commit hook `unset`s the git-local env before `cargo test`, and a plain `cargo test` / `make check` never had it. The loop removes vars that are absent.\n\nSo it is cargo-cult: it reads as if test isolation depends on it when it does not, and it obscures that the real guarantee lives inside `bl`.\n\n# Scope\n\nRemove the `for var in GIT_ENV_VARS { c.env_remove(var) }` loop from the three `Command::cargo_bin(\\\"bl\\\")` builders:\n\n- `tests/common/mod.rs` — `bl()` and `bl_as()`\n- `tests/init.rs` — the `bl_fresh` closure\n\n# Out of scope / keep\n\n`GIT_ENV_VARS` itself stays. `tests/init.rs` still has three *raw* `git` `StdCommand` calls (one `git init`, two `config --unset`) that genuinely need the scrub — real `git`, not the `bl` binary. A fuller tidy could route those through `git::clean_git_command` too and then retire the `GIT_ENV_VARS` re-export entirely; reasonable, but a separate call — do not pull it into this ball.\n\n# Origin\n\nSpotted while delivering bl-d1db (test git-helper consolidation, c3d7b78 on main): bl-d1db rebuilt `tests/common`'s git helpers on `clean_git_command` and re-exported `GIT_ENV_VARS`, which made this dead scrub visible.",
  "external": {"github-issues":{"issue":{"last_synced_status":"in_progress","number":100,"source":"balls","state":"open","synced_at":"2026-05-21T04:13:42.091807594+00:00","url":"https://github.com/mudbungie/balls/issues/100"}}},
  "id": "bl-3ce9",
  "links": [],
  "parent": null,
  "priority": 3,
  "repo": "git@github.com:mudbungie/balls.git",
  "status": "review",
  "synced_at": {"github-issues":"2026-05-21T04:13:42.110019823Z"},
  "tags": ["hygiene","testing"],
  "title": "Drop dead GIT_* scrub before the bl binary in tests",
  "type": "task",
  "updated_at": "2026-05-21T04:19:16.228456042Z"
}
