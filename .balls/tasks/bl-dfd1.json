{
  "branch": null,
  "claimed_by": null,
  "closed_at": null,
  "closed_children": [],
  "created_at": "2026-05-24T05:56:50.316325819Z",
  "delivered_in": null,
  "depends_on": [],
  "description": "Sibling to bl-ed32 under bl-f021. bl-ed32 landed SPEC-workspace-layout.md (commit aa26cc0) with a 'two project.json' / 'workspace.json' design that conflates the redirect signal with workspace-owned config. A design discussion (2026-05-23) settled on a cleaner three-file model. This ball amends the SPEC accordingly.\n\n## Why this is its own ball, not an in-place edit\n\nbl-ed32 already shipped — the SPEC is a delivered artifact with conformance tests downstream balls will gate against. Amending in place would silently move the contract; landing the revision as its own ball keeps the diff auditable and preserves the lineage 'we shipped A, then learned, then shipped B.' Same pattern as bl-2b49 revising bl-40e9's §16.13.\n\n## The three-file model\n\nThree files, each named for its layer's scope. 'Specific wins' as the precedence principle.\n\n- **tracker.json** — pointer-only. Lives on the workspace's own `origin balls/tasks` branch.\n  - Present ⇒ this branch redirects; the real tracker is at `state_url` / `state_branch`.\n  - Absent ⇒ this branch IS the tracker (solo case, or this branch is itself a federated tracker for other workspaces).\n  - Schema is *only* `state_url` and `state_branch`. Nothing else.\n- **repo.json** — per-repo config. Lives on the workspace's own `origin balls/tasks` branch alongside (or instead of) tracker.json.\n  - Carries per-code-repo stances: `delivery` (renamed — see below), `review.pre_check` (renamed), `require_remote_on_claim` / `_review` / `_close`, `auto_fetch_on_ready`, `stale_threshold_seconds`, `worktree_dir`, `protected_main`.\n  - `target_branch` is REMOVED at the repo level (see below).\n- **project.json** — project config. Lives on the federated tracker's `balls/tasks` branch (which is the workspace's own balls/tasks in the solo case).\n  - Carries project-owned fields: `version`, `id_length`, `min_bl_version`, `plugins` (config + participant policy).\n  - May also carry repo-overlapping fields (delivery, pre_check, require_remote_*) as project-wide *defaults*; repo.json overrides per the precedence rule below.\n\n### Precedence\n\n'Specific wins.' For fields that legitimately exist at both layers (delivery, pre_check, require_remote_*, etc.): repo.json's value wins; if absent there, project.json's value wins; if absent there, built-in default. For fields that are project-only by nature (min_bl_version, id_length, version): only project.json defines them; repo.json carrying them is an error (or ignored — TBD).\n\n### Solo case\n\nOn a solo workspace, tracker.json is *absent* (you've arrived); repo.json + project.json + tasks all coexist on the same `balls/tasks` branch at distinct paths under `.balls/`. The bl-e609 schema split holds via 'which file the field lives in,' not 'which branch.'\n\n## Requirements\n\n1. **Remove repo-level `target_branch`.** Not a platform limitation — workflow preference. The per-task `target_branch` override (already in the task schema) is the smallest unit that expresses the git-flow choice; the repo-level default is redundant. Default behavior is 'squash onto the branch the workspace root has checked out' (`git_current_branch(root)`). git-flow users either `git checkout develop` before `bl claim`, or use the per-task override. The fallback chain becomes `task.target_branch ?? HEAD@root` (the middle term `config.target_branch` is dropped).\n\n2. **Rename `delivery` and its variants.** Current: `delivery = local-squash` / `deferred`. Both names underspecify what the field actually does (delivery of what? deferred to when?). Shipped in 0.4.0 (2026-05-20), but the XDG pivot is already a breaking change to the file layout — a rename ridealong costs nothing extra. SPEC must propose at least two candidate name sets (with rationale for each), pick one, and gate the choice with a conformance test. Suggested directions: `integrate: { mode: direct | forge-pr }`, or `merge: { style: squash | forge-gated }`, or similar. The criterion: a new reader should understand what the field controls without context.\n\n3. **Rename `review.pre_check`.** Same treatment. Current `pre_check` is okay but ambiguous (pre-check of what?). Candidates: `review.gate_command`, `review.check_command`, `integrate.gate` (if the field belongs under the renamed delivery block). Pick one; gate with a conformance test.\n\n4. **Migration table extension.** SPEC §11 (Migration) gains rows for the file split:\n   - `.balls/workspace.json` (from bl-ed32's now-superseded design) → split into `tracker.json` (if it carried state_url) + `repo.json` (everything else workspace-owned).\n   - Pre-XDG `.balls/config.json` on `main` → split the same way during `bl migrate`.\n   - Pre-XDG `.balls/project.json` on tracker → keep the name, drop any fields that have moved to repo.json per the new precedence.\n\n5. **Field tables.** SPEC §6 gains an explicit three-column field table (tracker.json / repo.json / project.json) listing every field and its owner. No field has more than one *primary* owner. Fields that may appear as project-wide defaults overridable by repo are marked explicitly (e.g. 'delivery: primary owner = repo.json; project.json default permitted, repo.json wins').\n\n6. **Bootstrap convention restatement.** SPEC §5 simplifies: `bl` fetches `origin balls/tasks` first. If `tracker.json` is present and carries `state_url`, follow the redirect (single-hop, as before). Otherwise this branch is the tracker; proceed. The 'workspace.json with optional redirect' formulation is replaced by 'tracker.json present-or-absent.'\n\n7. **Conformance tests in §14 updated.** Re-cast existing tests for the three-file model; add tests for:\n   - tracker.json absence on a solo workspace.\n   - tracker.json with no other fields (it really is pointer-only — extra fields are rejected or stripped).\n   - repo.json + project.json coexistence on a solo workspace's balls/tasks branch.\n   - Precedence: a field set in both repo.json and project.json reads the repo.json value.\n   - Project-only fields (min_bl_version, id_length, version) appearing in repo.json: rejected or ignored (pick one in the SPEC).\n\n8. **Header note on bl-ed32's SPEC.** SPEC-workspace-layout.md gets a 'Superseded by SPEC-workspace-layout-v2.md' header (or this ball rewrites SPEC-workspace-layout.md in place and lifts bl-ed32's text into a 'history' appendix — author's call, but the contract being amended must be clear from a future read).\n\n9. **Defer the terminology sweep.** This ball uses 'workspace' as bl-ed32 / bl-1d46 do. bl-fc50 sweeps 'workspace' → 'clone' across all SPECs and code in a separate pass, AFTER this ball lands. Per bl-fc50's own sequencing note, doing both at once would touch the same paragraphs twice.\n\n## Out of scope\n\n- The dual-read implementation (separate child of bl-f021).\n- The `bl migrate` command (separate child).\n- `min_bl_version` bump (separate child, last in sequence).\n- The terminology sweep (bl-fc50).\n- Code changes to support the three-file model. SPEC only.\n\n## Deliverable\n\nAn updated SPEC-workspace-layout.md (or sibling SPEC-workspace-layout-v2.md superseding the first; author's call) carrying the three-file model, the rename, the removal, and the updated field tables and conformance tests. SPEC-tracker-state.md's supersession note carries forward; SPEC-workspace-layout.md (if superseded by a sibling) gets its own supersession note.",
  "external": {"github-issues":{"issue":{"last_synced_status":"open","number":134,"source":"balls","state":"open","synced_at":"2026-05-24T05:56:51.074321802+00:00","url":"https://github.com/mudbungie/balls/issues/134"}}},
  "id": "bl-dfd1",
  "links": [],
  "parent": "bl-f021",
  "priority": 3,
  "repo": "git@github.com:mudbungie/balls.git",
  "status": "open",
  "synced_at": {"github-issues":"2026-05-24T05:56:51.097095183Z"},
  "tags": [],
  "title": "Rewrite SPEC-workspace-layout under the three-file model + delivery/pre_check rename",
  "type": "docs",
  "updated_at": "2026-05-24T05:56:51.097104591Z"
}
